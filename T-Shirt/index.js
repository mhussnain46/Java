function make_shirt(size, message) {
  console.log(
    `The size of shirt should be ${size} and it should contain a message "${message}"`
  );
}
make_shirt("small", "I am not in danger. I am the danger");
